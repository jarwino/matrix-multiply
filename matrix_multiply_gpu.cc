#include "matrix_multiply_gpu.h"

void multiply_matrices_gpu(double a[N][M], double b[M][P], double c[N][P]) {

/*
  for (int i = 0; i < N; i++) {
    for (int j = 0; j < P; j++) {
      double sum = 0;
      for (int k = 0; k < M; k++) {
        sum += a[i][k] * b[k][j];
      }
      c[i][j] = sum;
    }
  }
  */

  return;

}
